# Database (Neon Postgres)
DATABASE_URL="postgresql://user:password@ep-xxxxx.us-east-1.aws.neon.tech/scrap-to-scale?sslmode=require"

# JWT Secret - change in production!
JWT_SECRET="scrap-to-scale-super-secret-jwt-key-change-this"

# App URL (used for generating judge access links)
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Node environment
NODE_ENV="development"
